# Copy to .env.local and set once the RSVP backend is deployed.
# Left unset, the RSVP form uses a mock submission handler.
NEXT_PUBLIC_RSVP_API_URL=https://api.example.com
NEXT_PUBLIC_WEDDING_SLUG=brian-chimango
